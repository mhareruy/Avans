{% extends 'base.html.twig' %}

{% block body %}
    <h1>Bestelorder</h1>

    <table>
        <tbody>
            <tr>
                <th>Aantal</th>
                <td>{{ bestelorder.aantal }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ bestelorder.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('bestelorder_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('bestelorder_edit', { 'id': bestelorder.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
